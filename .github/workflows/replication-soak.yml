name: Replication Soak

on:
  workflow_dispatch:
    inputs:
      duration_ms:
        description: Soak test duration in milliseconds
        required: false
        default: '600000'
  schedule:
    - cron: '17 3 * * *'

permissions:
  contents: read

concurrency:
  group: replication-soak-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  soak:
    name: Soak (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        node-version: [22, 24]

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Replication soak
        env:
          SIRANNON_SOAK_DURATION_MS: ${{ inputs.duration_ms || '600000' }}
        run: pnpm nx run sirannon-ts:test:soak
